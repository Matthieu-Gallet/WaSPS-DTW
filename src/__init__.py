"""
Source package for WaSPS-DTW.

Sub-packages:
  classification  — barycenter and distance functions for classification
  dataloader      — dataset-agnostic I/O and preprocessing (Format A/B)
  data_generator  — synthetic exponential series generation
  estimator       — MLE / log-cumulant parameter estimation
  optimizer       — SGD barycenter and prediction
  utils           — shared utilities
"""

__version__ = "2.0.0"
